rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write for now to ensure smooth migration
      // In a real app, you'd restrict this based on request.auth.uid
      allow read, write: if true;
    }
  }
}
